import { Routes } from "@angular/router";
import { ElevatorComponent } from "./elevator.component";

export const elevatorRoutes : Routes = [
    {
        path:'',
        component:ElevatorComponent
    }
]